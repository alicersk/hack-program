#!/usr/bin/env python

from setuptools import setup

setup(
    name="how_old",
    version="0.0.1",
    packages=[],
    entry_points={
        'console_scripts': ['how_old = how_old.__main__:main']
    }
)